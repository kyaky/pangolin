/target
**/*.rs.bk
.vscode/
.idea/
*.swp
*.swo
.DS_Store
.claude/

# Project-internal docs that should not be public.
CLAUDE.md
/_refs/

# Dev-tool session marker (dropped by external CLI tooling).
.codex

# Secrets / runtime artifacts.
.env
.env.*
*.log
*.token
*.pem
*.key
*.p12
*.pfx
config.toml
/run/

# Live-test log dump (`script /path/.pgn-logs/pgn-runN.log …`).
# Matches /tmp/pgn-run*.log historical convention but lives in the
# project dir so the AI pair can SSH-grep it by fixed path.
/.pgn-logs/
