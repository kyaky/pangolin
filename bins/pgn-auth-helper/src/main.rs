//! `pgn-auth-helper` — optional SAML WebView helper process.
//!
//! Task 1.1 scaffold. Real WebView bring-up lands in Phase 2.

fn main() -> anyhow::Result<()> {
    eprintln!("pgn-auth-helper: not yet implemented");
    Ok(())
}
